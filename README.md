# BERT-Encoder-Decoder-LLM
Developed a BERT encoder-decoder model to automatically summarize messenger conversations using the SAMSum dataset. The project includes data preprocessing, transformer fine-tuning, ROUGE evaluation, and analysis of model performance for automated dialogue summarization.
